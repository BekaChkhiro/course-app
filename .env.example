# This is the root .env.example file
# Copy this to .env and fill in the values
# Each workspace also has its own .env.example file

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/course_platform?schema=public

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Note: See individual workspace .env.example files for more configuration options:
# - apps/web/.env.example (Frontend configuration)
# - apps/api/.env.example (Backend configuration)
# - packages/database/.env.example (Database configuration)
